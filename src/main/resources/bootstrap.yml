spring:
  profiles:
    active: dev
  application:
    name: service-item
#  cloud:
#    config:
#      uri: http://localhost:8888
management:
  endpoints:
    web:
      exposure:
        include: "*"
